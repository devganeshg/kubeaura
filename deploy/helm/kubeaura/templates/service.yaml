apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeaura.fullname" . }}
  labels:
    {{- include "kubeaura.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kubeaura.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
