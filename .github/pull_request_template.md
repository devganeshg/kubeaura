## What this changes

<!-- The user-facing change, in a sentence or two. -->

## Why

<!-- The problem it solves. Link an issue if there is one. -->

## Checklist

- [ ] `make vet && make test` passes
- [ ] `gofmt -l .` prints nothing
- [ ] Commits are signed off (`git commit -s`) per the DCO
- [ ] Dependencies changed? Ran `sh scripts/gen-licenses.sh` and committed the result
- [ ] UI still degrades gracefully when the AI assistant is disabled
